function u_barre= calcul_structure_3(u_barre,u,Dx,Dy,Phi,epsilon,mu_prime,gamma)
   N = size(b,1);
   [nb_lignes,nb_colonnes] = size(u_barre);
   fu_barre = fft(u_barre);
   fu_barre
   fu = fft(u);
   phiu = phi(fu_barr - fu);

   u_barre = reshape(u_barre,nb_lignes,nb_colonnes);
end